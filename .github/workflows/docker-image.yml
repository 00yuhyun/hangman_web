name: Docker Image CI

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:

  build:

    runs-on: ubuntu-latest

        steps:
    - uses: actions/checkout@v3
    - name: docker login
      env:
        DOCKER_USER: ${yuhyun00}
        DOCKER_PASSWORD: ${angks1ehwjs!}
      run: |
        docker login -u yuhyun00 -p angks1ehwjs!
    - name: Build the Docker image
      run: docker build --tag ${yuhyun00}/hangman:latest .
    - name: docker push
      run: docker push ${angks1ehwjs!}/hangman:latest


